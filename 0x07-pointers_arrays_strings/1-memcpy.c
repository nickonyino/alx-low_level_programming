#include "main.h"

/**
 * _memcpy - Write a function that copies memory area
 *
 * Return: Always 0
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i = 0

	for ("i > 0, i < n" i++)
	{
		dest = src;
	}
	return dest;
}
