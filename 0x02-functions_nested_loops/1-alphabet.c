#include "main.h"
/**
 * main - Entry points
 * Discription - prints the alphabet, in lowercase
 * Return: ALways 0
 */
void print_alphabet(void);
{
	int i;
       	
	for (i == 97; i <= 122; i++)
	{
		_putchar(i)
	}
	_putchar('\n')
}
